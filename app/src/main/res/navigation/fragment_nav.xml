<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/fragment_nav"
    app:startDestination="@id/home2">

    <fragment
        android:id="@+id/home2"
        android:name="com.example.wordsapps.home"
        android:label="home"
        tools:layout="@layout/home" >
        <action
            android:id="@+id/action_home2_to_huruf2"
            app:destination="@id/huruf2" />
    </fragment>
    <fragment
        android:id="@+id/huruf2"
        android:name="com.example.wordsapps.huruf"
        android:label="huruf"
        tools:layout="@layout/huruf" >
        <action
            android:id="@+id/action_huruf2_to_home2"
            app:destination="@id/home2" />
    </fragment>
</navigation>